import Composer from "../Composer/Composer";
import MessageBubble from "./MessageBubble";
const Thread = () => {

        return (
            <div>
                <Composer />
            </div>
    );
}

export default Thread;
